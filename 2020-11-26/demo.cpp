#include <bits/stdc++.h>
using namespace std;
typedef long long ll;
typedef unsigned long long ull;
#define ms(s,val) memset(s, val, sizeof(s))
const int inf = INT_MAX;

int T;
int main(int argc, char * argv[]){
	// pair<int,int> --> x, y

	// len

	for i in range all nodes
		for j in range all nodes except i
			pair<int,int> x1, y1; 
			p  ... x2, ye;
			curlen = (y2-y1)*(y2-y1) + (x2-x1)*(x2-x1)
			if(curlen < len){
				ansx1, ansx2
			}


	


    return 0;
}