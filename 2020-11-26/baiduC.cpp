#include <bits/stdc++.h>
using namespace std;
typedef long long ll;
typedef unsigned long long ull;
#define ms(s,val) memset(s, val, sizeof(s))
const int inf = INT_MAX;

int T;
string s;
int main(int argc, char * argv[]){
	cin >> s;
	stack<pair<int, bool>> st;
	bool flag = 0;
	int ans = 0;
	int n = s.size();
	for(int i = 0; i < n; i++){
		if(i + 2 < n and s.substr(i, 3) == "for") 
	}
    return 0;
}