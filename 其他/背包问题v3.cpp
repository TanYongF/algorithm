#include <iostream>
#include <queue>
#include <map>
#include <vector>
using namespace std;
typedef pair <int,int> P;
int N , K;

int weight[50001];
int value[50001];
int w_v[50001];
int main(){
	cin >> N >> K;
	for (int i = 1; i <= N; ++i)
	{
		scanf("%d %d", &weight[i], &value[i]);
	}

	
}